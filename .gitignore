node_modules
dist
.env
.env.local
*.log
.DS_Store

# verification scratch — the verified hashes are committed in
# tests/contracts.cells.test.ts, so these don't need to be tracked.
.oracle-venv/
.oracle.py
.oracle.ts
